{
  "name": "resume-curator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP server: search the accomplishment store + session-summary backlog via offline TF-IDF.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "selftest": "node dist/index.js --selftest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
